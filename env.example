# ============================================================================
# Shopify Configuration
# ============================================================================

# Shopify Store Domain (e.g., your-store.myshopify.com)
NEXT_PUBLIC_SHOPIFY_STORE_DOMAIN=your-store.myshopify.com

# Shopify Storefront API Access Token (public, exposed to browser)
NEXT_PUBLIC_SHOPIFY_STOREFRONT_ACCESS_TOKEN=your-storefront-token

# Shopify Admin API Access Token (server-side only, keep secret)
SHOPIFY_ADMIN_TOKEN=your-admin-token

# Shopify API Version (optional, defaults to latest)
NEXT_PUBLIC_SHOPIFY_API_VERSION=2024-01

# Shopify Product Variant IDs (for checkout)
# These map frame styles to Shopify product variants
NEXT_PUBLIC_SHOPIFY_FRAME_VARIANT_ID=gid://shopify/ProductVariant/12345678
NEXT_PUBLIC_SHOPIFY_MAT_VARIANT_ID=gid://shopify/ProductVariant/87654321

# ============================================================================
# Cloudflare R2 CDN Configuration
# ============================================================================

# Option 1: Use separate URLs for each bucket (Recommended)
# Shared Assets CDN URL (frames, mats, comic, diploma, military, puzzle-frames, etc.)
# This is the public URL for your cfs-shared-assets bucket
# Example: https://cfs-shared-assets.yourdomain.com
# Or: https://pub-xxxxx.r2.dev (Cloudflare R2 public URL)
NEXT_PUBLIC_CDN_SHARED_URL=https://cfs-shared-assets.yourdomain.com

# Store-A Specific Assets CDN URL (assets/brand, assets/hero, assets/canvas, etc.)
# This is the public URL for your cfs-store-a-assets bucket
# Example: https://cfs-store-a-assets.yourdomain.com
# Or: https://pub-yyyyy.r2.dev (Cloudflare R2 public URL)
NEXT_PUBLIC_CDN_STORE_A_URL=https://cfs-store-a-assets.yourdomain.com

# Option 2: Use single base URL (Alternative)
# If you have a single CDN domain that serves both buckets, use this instead:
# NEXT_PUBLIC_CDN_BASE_URL=https://cdn.yourdomain.com
# The script will automatically append bucket names (cfs-shared-assets, cfs-store-a-assets)

# ============================================================================
# Store Configuration
# ============================================================================

# Store ID (for multi-store setup)
NEXT_PUBLIC_STORE_ID=store-a

# ============================================================================
# Error Monitoring (Sentry)
# ============================================================================

# Sentry DSN (Data Source Name) for error reporting
# Get this from your Sentry project settings
SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0

# Sentry Auth Token (for source map uploads, CI/CD only)
# SENTRY_AUTH_TOKEN=sntrys_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Sentry Organization slug
# SENTRY_ORG=your-org

# Sentry Project slug
# SENTRY_PROJECT=framecraft

# ============================================================================
# Feature Flags
# ============================================================================

# Enable debug logging for Shopify API requests
ENABLE_SHOPIFY_LOGGING=false

# Enable rate limit bypass for development
# DISABLE_RATE_LIMITING=false

# ============================================================================
# External Services
# ============================================================================

# Image Processing Service (if using external upscaling)
# IMAGE_SERVICE_API_KEY=your-image-service-api-key
