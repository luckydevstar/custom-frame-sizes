/**
 * Navigation Components
 *
 * Navigation components for Header and mobile navigation.
 * Note: These components depend on navigation configs which should be passed as props
 * from the app-level configuration.
 */

export { SearchBar } from "./SearchBar";
export type { SearchBarProps } from "./SearchBar";
