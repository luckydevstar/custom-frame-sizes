/**
 * Product Types
 *
 * Type definitions for FrameCraft products:
 * - FrameStyle: Frame style definitions
 * - MatColor: Mat color definitions
 * - GlassType: Glass type definitions
 * - FrameConfiguration: Complete frame configuration
 * - PricingBreakdown: Pricing calculation results
 *
 * Types will be extracted from client/src/types/products.ts in P1-023
 */

// Types will be defined here once extracted
// Example:
// export interface FrameStyle { ... }
// export interface MatColor { ... }
// export interface GlassType { ... }
// export interface FrameConfiguration { ... }
// export interface PricingBreakdown { ... }

// Placeholder export to make this a valid module
export {};
